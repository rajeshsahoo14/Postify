<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="bg-red-500 px-3 py-2 rounded-md text-sm" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl">Hello, <%=user.name %>
        </h3>
        <h5 class="mb-5">you can create a new post</h5>
        <form action="/post" method="post">
            <textarea placeholder="what's on your mind"
                class=" mb-2 w-1/3 p-3 outline-none resize-none block bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"></textarea>
            <input class="px-3 py-3 w-40 text-sm block bg-blue-500 rounded-md" type="submit" value="Create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(post => { %>
                    <div class="post w-1/3 p-4 mb-4 rounded-md bg-zinc-800 border[1px] border-zinc-700">
                    <h4 class="text-blue-500 mb-2"> @<%=user.username%> </h4>
                    <p class="text-sm tracking-tight"> <%= post.content%> </p>
                    <small class="mt-2 inline-block"><%= (post.likes && post.likes.length) || 0 %> likes</small>
                    <div class="btns flex mt-5 gap-5">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= (post.likes || []).indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            <a class="text-zinc-600" href="/edit/<%= post._id%>">Edit</a>
                    </div>
                </div>                    
               <% }) %>
            </div>
        </div>
    </div>
</body>

</html>