<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="bg-red-500 px-3 py-2 rounded-md text-sm" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%=post._id%>" method="post">
            <textarea placeholder="what's on your mind"
                class=" mb-2 w-1/3 p-3 outline-none resize-none block bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"><%= post.content%></textarea>
            <input class="px-3 py-3 w-40 text-sm block bg-yellow-500 text-black rounded-md" type="submit" value="Update post">
        </form>
    </div>
</body>

</html>